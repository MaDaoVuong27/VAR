# [Tên paper / repo]

- **Link gốc**: (arxiv/paper link, GitHub repo)
- **Ý tưởng chính**: (2-3 câu tóm tắt kỹ thuật cốt lõi)
- **Có thể áp dụng cho bài toán này ở đâu**: (liên hệ cụ thể tới `docs/IDEAS.md` — ví dụ: dùng cho candidate retrieval ICD-10, dùng cho negation detection...)
- **Giới hạn / không phù hợp**: (vd: cần model > 9B params, cần API ngoài, chỉ hỗ trợ tiếng Anh, cần fine-tune data không có...)
- **Trạng thái**: chưa đọc / đang đọc / đã đọc — đánh giá xong

## Code

Đặt code đã pull về trong `code/`. Nếu repo gốc nặng (checkpoint, dataset lớn), chỉ giữ phần code cần đọc, không commit weights/data vào đây — ghi rõ link gốc để tải lại khi cần.
