# Hướng dẫn cho AI agent làm việc trong repo này

Xem [`CLAUDE.md`](CLAUDE.md) — đó là nguồn duy nhất, đầy đủ (cấu trúc repo, vai trò từng thư mục, cách tương tác với `docs/`, workflow). File này chỉ là pointer để các tool đọc `AGENTS.md` theo convention (Codex...) tìm đúng chỗ, tránh trùng lặp nội dung.
